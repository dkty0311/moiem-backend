# 서버에 .env 파일을 만들고 아래 값을 설정하세요.
# cp .env.example .env

# MariaDB (MySQL) 연결 설정
# 형식: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="mysql://moiem_user:YOUR_PASSWORD@YOUR_SERVER_IP:3306/moiem"

JWT_SECRET="반드시-긴-랜덤-문자열로-변경"
JWT_EXPIRES_IN="15m"
REFRESH_TOKEN_EXPIRES_IN="7d"
PORT=3000
