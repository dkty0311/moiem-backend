# 서버에 .env 파일을 만들고 아래 값을 설정하세요.
# cp .env.example .env

# SQLite: prisma 폴더 기준 상대경로 또는 절대경로
# 로컬: file:./dev.db
# 리눅스 서버(권장): file:/var/lib/moiem/prod.db
DATABASE_URL="file:./dev.db"

JWT_SECRET="반드시-긴-랜덤-문자열로-변경"
JWT_EXPIRES_IN="15m"
REFRESH_TOKEN_EXPIRES_IN="7d"
PORT=3000
